# arxiv_hunt configuration
# Copy this file to .env and adjust as needed

# Default search parameters
ARXIV_DEFAULT_DAYS=7
ARXIV_DEFAULT_MAX_RESULTS=10

# Rate limit (seconds between API calls)
ARXIV_RATE_LIMIT=3.0

# Output directories
CSV_OUTPUT_DIR=data/csv
EXCEL_OUTPUT_DIR=data/excel
